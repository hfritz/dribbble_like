<fieldset>
  <legend>Upload a picture</legend>
  <%= form_for @user, :html => {:multipart => true} do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user_session from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="control-group">
      <%= f.label :pic, :class => 'control-label' %>
      <div class="controls">
        <%= f.file_field :pic, :class => 'text_field' %>
      </div>
    </div>
    
    <div class="form-actions">
      <%= f.submit "Upload Pic", :class => 'btn btn-primary' %>
    </div> 
  <% end %>

</fieldset>